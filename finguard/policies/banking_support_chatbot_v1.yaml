policy_id: banking_support_chatbot_v1
risk_level: medium

pii:
  enabled: true
  fast_pii_only: true
  entities: [IN_PAN, IN_AADHAAR, PHONE_NUMBER]

injection:
  enabled: true
  threshold: 0.95

output:
  on_fail: pass # Don't block customers, just anonymize
  redact_output: true
